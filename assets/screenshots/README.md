# Screenshots

Current repository screenshots:

```text
linux-dashboard.PNG
linux-files.PNG
linux-telegram.PNG
windows-dashboard.PNG
windows-script.PNG
windows-telegram.PNG
windows-terminal.PNG
```

These files are embedded directly in the root `README.md` and `README.id.md`.

Before adding or replacing screenshots:

- remove Telegram bot tokens;
- remove dashboard login tokens;
- remove passwords/API keys;
- review terminal output;
- review private paths, usernames, IP addresses, and URLs;
- keep filenames stable when possible so README links do not need to change;
- keep images reasonably compressed.
